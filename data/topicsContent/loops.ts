export const loops = `# Resumo de Conteúdos

### Laços de Repetição

Um laço FOR é uma estrutura que permite repetir um bloco de código um número específico de vezes.

Componentes de Sintaxe:

**Inicialização**: Executada apenas uma vez, no início

**Condição**: Verificada antes de cada iteração

**Incremento**: Executado após cada iteração

\`\`\`java
for(inicialização; condição; incremento){
	//Código que vai executar de forma repetitiva;
}

// Imprimindo números de 0 a 10
for(int i = 0; i <= 10; i++){
	System.out.println("Numero: " + i);
}

// Imprimindo números de 10 a 0
for(int i = 10; i >= 0; i--){
	System.out.println("Numero: " + i);
}
\`\`\`
`;
